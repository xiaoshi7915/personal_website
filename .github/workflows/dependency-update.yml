name: Dependency Update Check

on:
  schedule:
    # 每周一检查依赖更新
    - cron: '0 0 * * 1'
  workflow_dispatch:

jobs:
  check-dependencies:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Check for outdated packages
        run: |
          npm outdated || true
          echo "Dependency check completed"

      - name: Run security audit
        run: |
          npm audit --audit-level=moderate || true
          echo "Security audit completed"

      - name: Create issue for updates (optional)
        # 如果需要，可以创建GitHub Issue来提醒更新依赖
        # uses: actions/github-script@v7
        # with:
        #   script: |
        #     // 创建Issue的代码
        run: echo "Issue creation skipped - configure if needed"

